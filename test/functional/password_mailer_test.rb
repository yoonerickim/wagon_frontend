require 'test_helper'

class PasswordMailerTest < ActionMailer::TestCase
  #test "reset" do
  #  user = Factory.create(:user)
  #  user.password_reset_token = SecureRandom.urlsafe_base64(64)
  #  mail = PasswordMailer.reset(user)
  #  assert_equal "Hit the Spot Password Reset Requested", mail.subject
  #  assert_equal [user.email], mail.to
  #  assert_equal ["do-not-reply@hitthespot.com"], mail.from

  #  # TODO test mail.encoded
  #end

end
