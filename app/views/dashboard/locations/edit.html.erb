<div id="content" class="dashboard">
	<div id="vendor_nav">
		<ul>
      <li class="selected"><%= link_to "Edit Location", '#' %></li>
			<li><%= link_to 'Dashboard', dashboard_path %></li>			
		</ul>
	</div>
  <div id="content_wrap">
	  <%=raw '<p style="color: green; font-size: 14px;">' + flash[:notice] + '</p>' if flash.present? and flash[:notice].present? %>
	  <h2><%= @location.vendor.name %> <span><%= @location.address.street1 %></span></h2>
    <%= render 'form' %>
    <br><br>
  </div>
  <!--end content_wrap-->
</div>
<!--end content-->
