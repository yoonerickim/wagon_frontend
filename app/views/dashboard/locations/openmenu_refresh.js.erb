$('li#location-' + <%= @location.id %>).
  html("<%=j render 'dashboard/vendors/location', location: @location %>");

alert("<%= @location.name %> was updated from OpenMenu <%= @test_only_message %>");
