$('#location_overlay').html("<%=j render 'locations/menus', :location => @location %>");
$('.subtotal div.order_btn').html("<%=j menu_action(@order, :place_order_link) %>");

<%= render 'locations/menu_lightbox' %>
<%= render 'locations/menu_logic' %>

// Greg - This is where I started to transfer.
// The overlay stuff does not need to be mixed in with the map js, etc.
// Delete this comment after resolution.
//
// was in home.js
var location_overlay = jQuery("#location_overlay");
location_overlay.fadeIn(400);
location_overlay.vAlign();
// end was in home.js
